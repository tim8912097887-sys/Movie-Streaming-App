import LoginPresenter from "../components/layout/login/LoginPresenter";
import PageContainer from "../components/ui/PageContainer";

const LoginPage = () => {
  return (
    <PageContainer>
      <LoginPresenter />
    </PageContainer>
  );
};

export default LoginPage;
